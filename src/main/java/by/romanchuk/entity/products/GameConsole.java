package by.romanchuk.entity.products;

import by.romanchuk.entity.Product;

public class GameConsole extends Product {
    public GameConsole(int id, String name, double price, int count) {
        super(id, name, price, count);
    }
}
